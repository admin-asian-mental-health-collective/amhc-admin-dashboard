import Button from '@/components/Button';

const Home = () => {
	return (
		<div className="Home">
			<Button onClick={() => console.log(true)}>Test 123</Button>
		</div>
	);
};

export default Home;
